**Schema (MySQL v5.7)**

    CREATE TABLE orders (
        order_id INT,
        customer_id INT,
        amount INT
    );
    
    INSERT INTO orders VALUES
    (1, 1, 500),
    (2, 1, 300),
    (3, 2, 700),
    (4, 2, 200),
    (5, 2, 100),
    (6, 3, 50);

---

**Query #1**

    SELECT
    	order_id,
        customer_id
    FROM orders;

| order_id | customer_id |
| -------- | ----------- |
| 1        | 1           |
| 2        | 1           |
| 3        | 2           |
| 4        | 2           |
| 5        | 2           |
| 6        | 3           |

---
**Query #2**

    SELECT
    	order_id,
        customer_id,
        amount
    FROM orders
    WHERE amount > 400;

| order_id | customer_id | amount |
| -------- | ----------- | ------ |
| 1        | 1           | 500    |
| 3        | 2           | 700    |

---
**Query #3**

    SELECT
    	COUNT(*) AS total_orders
    FROM orders;

| total_orders |
| ------------ |
| 6            |

---
**Query #4**

    SELECT
    	COUNT(customer_id) AS total_nenull_orders
    FROM orders;

| total_nenull_orders |
| ------------------- |
| 6                   |

---
**Query #5**

    SELECT
    	customer_id,
        COUNT(*) AS total_orders
    FROM orders
    GROUP BY customer_id
    ORDER BY customer_id ASC;

| customer_id | total_orders |
| ----------- | ------------ |
| 1           | 2            |
| 2           | 3            |
| 3           | 1            |

---
**Query #6**

    SELECT
    	customer_id,
        COUNT(*) AS total_orders
    FROM orders
    GROUP BY customer_id
    HAVING COUNT(*) > 2
    ORDER BY total_orders DESC;

| customer_id | total_orders |
| ----------- | ------------ |
| 2           | 3            |

---

**Schema (MySQL v5.7)**

    CREATE TABLE customers (
        customer_id INT,
        name VARCHAR(50)
    );
    
    INSERT INTO customers VALUES
    (1, 'Anna'),
    (2, 'Petr'),
    (3, 'Eva'),
    (4, 'Jan');
    CREATE TABLE orders (
        order_id INT,
        customer_id INT,
        amount INT
    );
    
    INSERT INTO orders VALUES
    (1, 1, 500),
    (2, 1, 300),
    (3, 2, 700),
    (4, 2, 200),
    (5, 2, 100);

---

**Query #1**

    SELECT
    	c.name,
        c.customer_id,
        o.order_id
    FROM customers c
    JOIN orders o
    	ON c.customer_id = o.customer_id
    ORDER BY c.name ASC;

| name | customer_id | order_id |
| ---- | ----------- | -------- |
| Anna | 1           | 1        |
| Anna | 1           | 2        |
| Petr | 2           | 4        |
| Petr | 2           | 5        |
| Petr | 2           | 3        |

---
**Query #2**

    SELECT
        c.name,
        c.customer_id
    FROM customers c
    LEFT JOIN orders o
        ON c.customer_id = o.customer_id
    WHERE o.order_id IS NULL;

| name | customer_id |
| ---- | ----------- |
| Eva  | 3           |
| Jan  | 4           |

---
**Query #3**

    SELECT
    	c.name,
        c.customer_id,
        COUNT(o.order_id) AS total_orders
    FROM customers c
    LEFT JOIN orders o
    	ON c.customer_id = o.customer_id
    GROUP BY
    	c.name,
        c.customer_id
    HAVING COUNT(o.order_id) = 0
    ORDER BY c.name ASC;

| name | customer_id | total_orders |
| ---- | ----------- | ------------ |
| Eva  | 3           | 0            |
| Jan  | 4           | 0            |

---
**Query #4**

    SELECT
    	c.name,
        c.customer_id,
        COALESCE(SUM(o.amount), 0) AS total_revenue
    FROM customers c
    LEFT JOIN orders o
    	ON c.customer_id = o.customer_id
    GROUP BY
    	c.name,
        c.customer_id
    ORDER BY
    	total_revenue DESC;

| name | customer_id | total_revenue |
| ---- | ----------- | ------------- |
| Petr | 2           | 1000          |
| Anna | 1           | 800           |
| Eva  | 3           | 0             |
| Jan  | 4           | 0             |

---

SELECT
    customer,
    category,
    revenue
FROM (
    SELECT
        customer,
        category,
        revenue,
        ROW_NUMBER() OVER (
            PARTITION BY customer
            ORDER BY revenue DESC
        ) AS rn
    FROM sales
) x
WHERE rn = 1;

SELECT
    customer,
    category,
    revenue
FROM (
    SELECT
        customer,
        category,
        revenue,
        ROW_NUMBER() OVER (
            PARTITION BY customer
            ORDER BY revenue DESC
        ) AS rn
    FROM sales
) x
WHERE rn =< 2;

SELECT
    customer,
    category,
    revenue
FROM (
    SELECT
        customer,
        category,
        revenue,
        RANK() OVER (
            PARTITION BY customer
            ORDER BY revenue DESC
        ) AS rnk
    FROM sales
) x
WHERE rnk = 1;

SELECT
    customer,
    revenue,
    CASE
        WHEN revenue > 1000 THEN 'top'
        WHEN revenue > 500 THEN 'medium'
        ELSE 'low'
    END AS segment
FROM sales;

**Schema (MySQL v5.7)**

    CREATE TABLE customers (
        customer_id INT,
        name VARCHAR(50)
    );
    
    INSERT INTO customers VALUES
    (1, 'Anna'),
    (2, 'Petr'),
    (3, 'Eva'),
    (4, 'Jan');
    
    CREATE TABLE orders (
        order_id INT,
        customer_id INT,
        amount INT
    );
    
    INSERT INTO orders VALUES
    (1, 1, 500),
    (2, 1, 300),
    (3, 2, 700),
    (4, 2, 200),
    (5, 2, 100);

---

**Query #1**

    SELECT
    	c.customer_id,
        c.name,
        COALESCE(SUM(o.amount), 0) AS total_revenue,
        CASE
        	WHEN COALESCE(SUM(o.amount), 0) > 1000 THEN 'top'
            WHEN COALESCE(SUM(o.amount), 0) > 500 THEN 'medium'
            ELSE 'low'
     	END AS segment
    FROM customers c
    LEFT JOIN orders o
    	ON c.customer_id = o.customer_id
    GROUP BY
    	c.customer_id,
        c.name
    ORDER BY total_revenue DESC;

| customer_id | name | total_revenue | segment |
| ----------- | ---- | ------------- | ------- |
| 2           | Petr | 1000          | medium  |
| 1           | Anna | 800           | medium  |
| 3           | Eva  | 0             | low     |
| 4           | Jan  | 0             | low     |


-- ============================================================
-- MINI TEST #1
-- Topic: SQL Basics + Window Functions
-- ============================================================

-- Instructions:
-- Answer the questions without running SQL if possible.
-- Focus on understanding, not memorization.

-- ============================================================
-- 1. FILL IN
-- ============================================================

-- Complete the window function:

-- LAG(revenue) OVER (
--     PARTITION BY ______
--     ORDER BY ______
-- )

-- Expected answer:
-- PARTITION BY customer
-- ORDER BY date


-- ============================================================
-- 2. UNDERSTAND OUTPUT
-- ============================================================

-- What does this return?

-- SUM(revenue) OVER ()

-- Answer:
-- Total revenue across the whole table, repeated on each row.


-- ============================================================
-- 3. GROUP BY vs WINDOW FUNCTION
-- ============================================================

-- Explain the difference:

-- GROUP BY
-- vs
-- SUM() OVER ()

-- Answer:
-- GROUP BY aggregates and reduces rows.
-- SUM() OVER () keeps all rows and adds aggregated value.


-- ============================================================
-- 4. JOIN LOGIC
-- ============================================================

-- How do you find customers WITHOUT orders?

-- Answer:
-- LEFT JOIN + IS NULL

-- Example:
-- FROM customers c
-- LEFT JOIN orders o
--     ON c.customer_id = o.customer_id
-- WHERE o.order_id IS NULL


-- ============================================================
-- 5. WHERE vs HAVING
-- ============================================================

-- When do you use HAVING?

-- Answer:
-- HAVING filters aggregated results (after GROUP BY).
-- WHERE filters rows before aggregation.


-- ============================================================
-- 6. WINDOW FUNCTION LOGIC
-- ============================================================

-- What does this do?

-- ROW_NUMBER() OVER (
--     PARTITION BY customer
--     ORDER BY date DESC
-- )

-- Answer:
-- Assigns row numbers per customer, starting from the latest date.


-- ============================================================
-- 7. RANK vs ROW_NUMBER
-- ============================================================

-- When do you use RANK()?

-- Answer:
-- When you want to preserve ties (same values share the same rank).


-- ============================================================
-- 8. PERCENT CALCULATION
-- ============================================================

-- Why use 100.0 instead of 100?

-- Answer:
-- Forces decimal calculation and prevents integer division.


-- ============================================================
-- 9. CTE (WITH)
-- ============================================================

-- What is WITH?

-- Answer:
-- A named intermediate result used in the query (CTE).


-- ============================================================
-- 10. ERROR DEBUGGING
-- ============================================================

-- Why does this NOT work?

-- SELECT
--     revenue,
--     revenue * 100.0 / SUM(revenue) OVER () AS pct
-- FROM sales
-- WHERE pct > 10;

-- Answer:
-- Alias 'pct' is not available in WHERE (SELECT runs later).

-- Correct solution:

-- WITH base AS (
--     SELECT
--         revenue,
--         revenue * 100.0 / SUM(revenue) OVER () AS pct
--     FROM sales
-- )
-- SELECT *
-- FROM base
-- WHERE pct > 10;


-- ============================================================
-- END
-- ============================================================

-- ============================================================
-- MINI TEST #2
-- Topic: JOINs & Filtering Logic
-- ============================================================

-- ============================================================
-- 1. LEFT JOIN vs INNER JOIN
-- ============================================================

-- What is the difference?

-- INNER JOIN
-- vs
-- LEFT JOIN

-- Answer:
-- INNER JOIN returns only matching rows.
-- LEFT JOIN returns all rows from the left table + matches (NULL if no match).


-- ============================================================
-- 2. LEFT JOIN + WHERE (TRICKY)
-- ============================================================

-- What does this do?

-- SELECT *
-- FROM customers c
-- LEFT JOIN orders o
--     ON c.customer_id = o.customer_id
-- WHERE o.amount > 100;

-- Answer:
-- Behaves like INNER JOIN (filters out NULLs).


-- ============================================================
-- 3. CORRECT FILTERING
-- ============================================================

-- How to keep all customers but only orders > 100?

-- Answer:

-- LEFT JOIN orders o
--     ON c.customer_id = o.customer_id
--    AND o.amount > 100


-- ============================================================
-- 4. FIND MISSING DATA
-- ============================================================

-- How to find customers with NO orders?

-- Answer:

-- LEFT JOIN orders o
--     ON c.customer_id = o.customer_id
-- WHERE o.order_id IS NULL


-- ============================================================
-- 5. COUNT + LEFT JOIN
-- ============================================================

-- Difference:

-- COUNT(*) vs COUNT(o.order_id)

-- Answer:
-- COUNT(*) counts all rows.
-- COUNT(column) counts only non-NULL values.


-- ============================================================
-- 6. JOIN DIRECTION
-- ============================================================

-- You want ALL orders + customers if exist.

-- What do you write?

-- Answer:

-- FROM orders o
-- LEFT JOIN customers c
--     ON c.customer_id = o.customer_id


-- ============================================================
-- 7. WHERE IS NULL
-- ============================================================

-- What does this return?

-- WHERE o.order_id IS NULL

-- Answer:
-- Rows where no match was found (no related record).


-- ============================================================
-- 8. COMMON MISTAKE
-- ============================================================

-- Why is this wrong?

-- LEFT JOIN orders o
--     ON c.customer_id = o.customer_id
-- WHERE o.amount > 100

-- Answer:
-- WHERE removes NULLs → breaks LEFT JOIN logic.


-- ============================================================
-- 9. RIGHT JOIN
-- ============================================================

-- Why is RIGHT JOIN rarely used?

-- Answer:
-- It can be replaced by LEFT JOIN by switching table order.


-- ============================================================
-- 10. BONUS
-- ============================================================

-- How to get customers WITH at least 1 order?

-- Answer:

-- INNER JOIN orders o
--     ON c.customer_id = o.customer_id

-- OR

-- LEFT JOIN + WHERE NOT NULL
-- WHERE o.order_id IS NOT NULL

-- ============================================================
-- END
-- ============================================================

-- ============================================================
-- MINI TEST #3
-- Topic: Window Functions (Advanced)
-- ============================================================

-- ============================================================
-- 1. PARTITION BY
-- ============================================================

-- What does PARTITION BY do?

-- Answer:
-- Splits data into groups for window calculations.


-- ============================================================
-- 2. ORDER BY in OVER()
-- ============================================================

-- What is the purpose?

-- Answer:
-- Defines calculation order (e.g. running total, LAG).


-- ============================================================
-- 3. RUNNING TOTAL
-- ============================================================

-- What does this do?

-- SUM(revenue) OVER (
--     PARTITION BY customer
--     ORDER BY date
-- )

-- Answer:
-- Running total per customer over time.


-- ============================================================
-- 4. SHARE OF TOTAL
-- ============================================================

-- What does this calculate?

-- revenue * 100.0 / SUM(revenue) OVER ()

-- Answer:
-- Share (%) of each row relative to total.


-- ============================================================
-- 5. SHARE WITHIN GROUP
-- ============================================================

-- What does this do?

-- revenue * 100.0 / SUM(revenue) OVER (PARTITION BY customer)

-- Answer:
-- Share (%) within each customer.


-- ============================================================
-- 6. MAX DIFFERENCE
-- ============================================================

-- What does this return?

-- MAX(revenue) OVER (PARTITION BY customer) - revenue

-- Answer:
-- Difference from max value within customer.


-- ============================================================
-- 7. LAG
-- ============================================================

-- What does LAG do?

-- Answer:
-- Returns previous row value.


-- ============================================================
-- 8. LEAD
-- ============================================================

-- What does LEAD do?

-- Answer:
-- Returns next row value.


-- ============================================================
-- 9. RANK vs ROW_NUMBER
-- ============================================================

-- Difference?

-- Answer:
-- RANK keeps ties.
-- ROW_NUMBER breaks ties.


-- ============================================================
-- 10. TRICKY
-- ============================================================

-- Why does this NOT preserve ties?

-- RANK() OVER (
--     ORDER BY revenue DESC, customer
-- )

-- Answer:
-- Because ORDER BY includes customer → values are no longer equal.


-- ============================================================
-- BONUS
-- ============================================================

-- What happens if ORDER BY is missing in LAG?

-- Answer:
-- Result is undefined (no defined order).

-- ============================================================
-- END
-- ============================================================


